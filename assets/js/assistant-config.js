// Public endpoint only. API keys must never appear in website assets.
// Leave empty until the separately hosted assistant service is ready.
window.SRS_ASSISTANT_CONFIG = Object.freeze({ endpoint: "" });
